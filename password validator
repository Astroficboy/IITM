char = input()
not_in = ['/', '\\ ', '=', "'", '"', ' ']
if len(char)>=8 and len(char)<=32:
    if char[0] is char.isupper() or char.islower():
        if not any(x in not_in for x in char):
            print(True)
        else:
            print(False)
